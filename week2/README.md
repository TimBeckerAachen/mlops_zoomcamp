The second week concerns MLflow a tool which can be used for experiment tracking and to build a model registry.

To start the MLflow UI you can use: `mlflow ui --backend-store-uri sqlite:///mlflow.db`
